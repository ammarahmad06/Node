// # Defines a function sub that logs the difference between two numbers.
// # Uses module.exports to export the sub function for use in other files.
const sub = (num1, num2) => {
    console.log("🚀 ~ sub ~ sub:", num1 - num2)
}
module.exports = {sub};
